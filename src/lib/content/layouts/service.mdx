<script>
  // MDsveX layout wrapper for service pages
  export let metadata = {};
</script>

import { getServiceJsonLd, COMPANY } from '$lib/seo';


<article class="prose lg:prose-xl max-w-4xl mx-auto py-12">
  <header>
    <p class="text-sm text-primary-600 font-semibold">Service</p>
    <h1>{metadata.title}</h1>
    {#if metadata.description}
      <p class="lead">{metadata.description}</p>
    {/if}
  </header>

  <!-- Service JSON-LD -->
  {@html `<script type="application/ld+json">${JSON.stringify(getServiceJsonLd(metadata.title || '', metadata.description || '', `${COMPANY.city}, ${COMPANY.state}`))}</script>`}

  <slot />

  {#if metadata.features}
    <section aria-labelledby="features-heading" class="mt-8">
      <h2 id="features-heading">Our Services Include</h2>
      <ul>
        {#each metadata.features as f}
          <li>{f}</li>
        {/each}
      </ul>
    </section>
  {/if}
</article>
