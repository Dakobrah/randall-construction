<script>
  import { getServiceJsonLd, COMPANY } from '$lib/seo';
  export let metadata = {};
</script>

<article class="max-w-4xl mx-auto py-12 px-4 md:px-8">
  {#if metadata.image}
    <div class="mb-8 rounded-2xl overflow-hidden shadow-md aspect-[16/9] bg-dark-100 dark:bg-dark-800">
      <img
        src={metadata.image}
        alt={metadata.title}
        loading="eager"
        width="1600"
        height="900"
        class="w-full h-full object-cover" />
    </div>
  {/if}

  <header class="mb-8">
    <p class="text-sm text-primary-600 dark:text-primary-400 font-semibold uppercase tracking-wide mb-2">Service</p>
    <h1 class="text-3xl md:text-4xl lg:text-5xl font-heading font-bold text-dark-900 dark:text-white mb-4">{metadata.title}</h1>
    {#if metadata.description}
      <p class="text-lg text-dark-500 dark:text-dark-300 leading-relaxed">{metadata.description}</p>
    {/if}
  </header>

  {@html `<script type="application/ld+json">${JSON.stringify(getServiceJsonLd(metadata.title || '', metadata.description || '', `${COMPANY.city}, ${COMPANY.state}`))}<\/script>`}

  <div class="text-dark-700 dark:text-dark-200 leading-relaxed space-y-4">
    <slot />
  </div>

  {#if metadata.features}
    <section aria-labelledby="features-heading" class="mt-10 p-6 bg-gray-50 dark:bg-dark-800 rounded-xl">
      <h2 id="features-heading" class="text-xl font-heading font-bold text-dark-900 dark:text-white mb-4">Our Services Include</h2>
      <ul class="space-y-2">
        {#each metadata.features as f}
          <li class="flex items-center gap-2 text-dark-600 dark:text-dark-300">
            <svg class="w-4 h-4 text-primary-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
            </svg>
            {f}
          </li>
        {/each}
      </ul>
    </section>
  {/if}

  <div class="mt-10">
    <a href="/request-quote" class="btn-primary text-lg px-8 py-4">Get a Free Estimate</a>
  </div>
</article>
