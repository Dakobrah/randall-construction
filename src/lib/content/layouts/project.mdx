<script>
  export let metadata = {};
</script>

<article class="prose lg:prose-xl max-w-4xl mx-auto py-12">
  <header>
    <p class="text-sm text-primary-600">Project</p>
    <h1>{metadata.title}</h1>
    {#if metadata.location}
      <p class="text-sm text-dark-500">{metadata.location}</p>
    {/if}
  </header>

  <slot />

  {#if metadata.specs}
    <section aria-labelledby="project-specs" class="mt-8">
      <h2 id="project-specs">Project Details</h2>
      <dl>
        {#each metadata.specs as spec}
          <div>
            <dt class="font-semibold">{spec.label}</dt>
            <dd>{spec.value}</dd>
          </div>
        {/each}
      </dl>
    </section>
  {/if}
</article>
